# auto_ssh_auther 현재 구현 반영 계획서

## 1. 프로젝트 목적
- 이 앱은 로컬 `~/.ssh` 안의 공개키를 선택해서, 사용자가 직접 입력한 원격 서버의 `authorized_keys`에 등록해주는 GUI 도구다.
- 첫 목표는 복잡한 운영 관리가 아니라, SSH 공개키 등록 작업을 간단하고 빠르게 처리하는 것이다.
- 데이터베이스나 JSON 기반 이력 저장은 넣지 않고, 세션 단위로 동작하는 가벼운 앱으로 유지한다.

## 2. 현재 구현 완료 범위
### GUI
- `PySide6` 기반 메인 윈도우 구현 완료
- 공개키 목록, 서버 입력 폼, 결과 로그 영역 구성 완료
- SSH 작업은 `QThread`로 실행되어 기본적인 UI 멈춤 방지 처리 완료

### 로컬 키 관리
- `~/.ssh/*.pub` 자동 탐색 구현 완료
- 공개키 타입, 코멘트 파싱 구현 완료
- 새 SSH 키 생성 기능 구현 완료
- 선택한 키 쌍 삭제 기능 구현 완료

### 원격 서버 작업
- 비밀번호 기반 SSH 접속 구현 완료
- 연결 테스트 기능 구현 완료
- `known_hosts` 기반 호스트 키 검증 및 미등록 호스트 자동 등록 적용 완료
- 원격 `~/.ssh` 디렉터리 자동 생성 구현 완료
- 원격 `authorized_keys` 읽기 구현 완료
- 동일 키 중복 등록 방지 구현 완료
- 공개키 추가 등록 기능 구현 완료

### 문서
- `README.md`에 현재 기능과 사용 흐름 반영 완료

### 패키징
- `PyInstaller` 기반 빌드 설정 구현 완료
- `auto_ssh_auther.spec` 추가 완료
- `build.py` 빌드 실행 스크립트 추가 완료
- 런타임 아이콘과 실행 파일 아이콘에 같은 리소스를 사용하도록 적용 완료
- 아이콘 파일이 빌드 결과물에 자동 포함되도록 설정 완료

## 3. 현재 코드 구조
- `main.py`
  - 앱 진입점
- `build.py`
  - `PyInstaller` 빌드 실행 스크립트
- `auto_ssh_auther.spec`
  - 플랫폼별 아이콘 포함 빌드 설정
- `src/ssh_auther/app_assets.py`
  - 아이콘 경로, 앱 이름, Windows AppUserModelID 처리
- `src/ssh_auther/ui/main_window.py`
  - 메인 윈도우
  - 키 생성 다이얼로그
  - 연결 테스트 / 키 등록 / 키 생성 / 키 삭제 버튼 처리
- `src/ssh_auther/keys/local_keys.py`
  - 공개키 탐색
  - 공개키 파싱
  - `ssh-keygen` 기반 키 생성
  - 키 파일 삭제
- `src/ssh_auther/ssh/remote.py`
  - `paramiko` 기반 SSH 연결
  - 원격 명령 실행
  - `authorized_keys` 읽기 / 추가
- `src/ssh_auther/services/register.py`
  - 키 중복 검사
  - 등록 흐름 제어
  - 접속 테스트 결과 메시지 정리

## 4. 실제 구현 기준 기능 목록
### 현재 가능한 기능
- 로컬 공개키 목록 자동 표시
- 공개키 생성
- 공개키 삭제
- 서버 연결 테스트
- 공개키 등록
- 이미 등록된 키 판별
- 원격 `.ssh` / `authorized_keys` 자동 준비
- PyInstaller 빌드
- 창 타이틀바 아이콘 적용
- 실행 파일 아이콘 적용

### 아직 없는 기능
- 원격 서버에서 키 제거
- 서버 정보 저장
- 최근 접속 서버 기억
- 키 기반 관리자 로그인

## 5. 리뷰 결과
### 좋은 점
- 현재 범위가 단순해서 목적과 구현이 잘 맞는다.
- 기능 분리는 `ui`, `keys`, `ssh`, `services` 정도로 깔끔하다.
- 키 등록 전에 연결 테스트를 따로 둘 수 있어서 사용자 입장에서 이해하기 쉽다.
- 키 생성/삭제까지 포함되어 실제 사용성은 계획보다 좋아졌다.

### 보완이 필요한 부분
- 타임아웃/연결 실패 예외 처리가 충분히 정확하지 않다.
  - 주요 케이스는 개선했지만, 실제 운영 환경에서 더 다양한 네트워크 예외 메시지를 다듬을 여지는 남아 있다.
- 자동 테스트 범위가 아직 좁다.
  - 현재는 문자열 처리와 오류 메시지 수준의 기본 테스트만 추가된 상태라, 실제 SSH 흐름 통합 테스트는 아직 없다.
- 원격 추가 로직은 더 명확하게 정리할 수 있다.
  - 개행 보정은 들어갔지만, 장기적으로는 원격 파일 처리 로직을 더 구조화할 여지는 있다.
- 문서와 실제 구현의 차이는 많이 줄었지만, 계획서 기준으로는 “생성/삭제 기능 포함”을 명시적으로 반영해야 했다.

## 6. 다음 보완 우선순위
1. 실제 SSH 흐름 통합 테스트 추가
2. 네트워크/인증 예외 메시지 정교화
3. 원격 키 제거 기능 추가 여부 결정
4. 플랫폼별 배포 절차 문서화 보강

## 7. v1 완료 기준
- 사용자가 로컬 공개키를 선택할 수 있어야 한다.
- 사용자가 서버 주소, 포트, 계정, 비밀번호를 입력할 수 있어야 한다.
- 연결 테스트가 동작해야 한다.
- 버튼 한 번으로 원격 `authorized_keys`에 키를 등록할 수 있어야 한다.
- 이미 등록된 키는 중복 추가되지 않아야 한다.
- 키 생성과 키 삭제가 로컬 `~/.ssh` 기준으로 정상 동작해야 한다.
- 실패 메시지가 사용자가 이해 가능한 수준이어야 한다.

## 8. 테스트 현황
### 현재 확인한 내용
- Python 컴파일 스모크 통과
- 오프스크린 GUI 초기화 스모크 통과
- `unittest` 기반 문자열 처리 / 오류 메시지 테스트 추가 및 통과
- macOS `PyInstaller` 빌드 통과
- `dist`에 아이콘 리소스 자동 포함 확인

### 아직 필요한 확인
- 실제 테스트 서버에 등록 성공 확인
- 잘못된 비밀번호 입력 시 메시지 확인
- 존재하지 않는 서버/포트 실패 처리 확인
- 중복 키 등록 시 안내 메시지 확인
- 생성한 키가 즉시 목록에 반영되는지 확인
- 삭제 시 공개키/비밀키가 함께 제거되는지 확인

## 9. 현재 결론
- 프로젝트는 “단순한 공개키 등록 GUI 앱” 방향으로 구현이 잘 진행된 상태다.
- 초기 설계에서 불필요했던 DB, JSON 저장소, 복잡한 운영 개념은 실제 구현에서 제거되어 방향이 더 좋아졌다.
- 다음 단계는 기능 확장보다 안정성 보강과 테스트 추가가 우선이다.
